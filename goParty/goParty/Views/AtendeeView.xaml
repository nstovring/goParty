<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:av="clr-namespace:goParty.Views"
             x:Class="goParty.Views.AtendeeView"
             >
  <ContentView.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <av:CardListView Grid.Row="0" Grid.RowSpan="2" ItemsSource="{Binding Attendees}" HorizontalOptions="FillAndExpand"/>

            <Image Grid.Row="0" Grid.Column="0" Source="backicon.png" VerticalOptions="Start" HorizontalOptions="Start">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding BackIconClickedCommand}"/>
                </Image.GestureRecognizers>
            </Image>
        </Grid>
    </ContentView.Content>
</ContentView>